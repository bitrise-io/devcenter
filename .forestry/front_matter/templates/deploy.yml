---
label: deploy
hide_body: false
fields:
- type: text
  name: title
  label: title
- type: field_group
  label: menu
  name: menu
  fields:
  - type: field_group
    name: deploy
    label: deploy
    fields:
    - type: number
      name: weight
      label: weight
